# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
*.whl
*.egg

# Environments
.venv/
venv/
env/

# Local state (never commit secrets or local DBs)
.knovaryn/
*.db
*.db-wal
*.db-shm

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
id_rsa*
id_ed25519*

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Docs build
site/
# Local build scratch (any variant — the wildcard deliberately does not
# enumerate names; local-only build/audit material never gets committed)
.knovaryn-build*/
knovaryn-demo/

# Private maintainer workspace: local-only working notes. Never committed
# or packaged.
.knovaryn-maintainer/
